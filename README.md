How to run the project.

```markdown
# NormalizerProject

This project normalizes job titles using Java and Maven.

## Prerequisites

- JDK 8 or higher
- Maven 3.6.0 or higher

## How to Run

1. **Clone the repository:**

   ```sh
   git clone https://github.com/atiliosud/Interview-Normalize-Project
   cd NormalizerProject
   ```

2. **Compile the project:**

   ```sh
   mvn clean compile
   ```

3. **Run the main class:**

   ```sh
   mvn exec:java -Dexec.mainClass=com.example.SimpleJobTitleNormalizer
   ```

4. **Run the tests:**

   ```sh
   mvn test
   ```
```
